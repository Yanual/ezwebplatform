Template: ezweb-platform-apache2-fastcgi/instances
Type: multiselect
Default: default
Choices: ${choices}
_Description: Select the instances of EzWeb you want to configure to use Apache2 and fastcgi
	The selected EzWeb instances will be configured to use Apache2 and
	fastcgi.
	.
	This will not enable the selected EzWeb instances, although they will
	continue to be enabled if they are currently enabled.

